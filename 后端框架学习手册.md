# 一、整体培养目标与教学设计思想

## 1. 培养目标（最终要把实习生带到哪）

完成本教学计划后，实习生应当能够：

1. 明确 **后端在整个系统中的角色**
2. 理解 **一次接口请求从浏览器到数据库的完整链路**
3. 独立完成：
   - Spring Boot 项目搭建
   - 接口编写
   - 参数接收与返回
   - MyBatis 操作数据库
4. 能够：
   - 看懂后端代码
   - 修改业务逻辑
   - 自主新增简单功能
5. 建立 **正确的后端工程思维**

------

## 2. 教学核心原则（给导师看的）

- ❌ 不追求“高深”
- ✅ 追求“能跑 + 能改 + 能理解”
- ❌ 不堆概念
- ✅ 每个概念都配**生活比喻**
- ❌ 不直接丢源码
- ✅ 每一步都讲“为什么”

------

## 3. 项目选型说明

### 技术栈

- Java 17（或 8 也可）
- Spring Boot
- MyBatis
- MySQL
- Maven

### 示例项目

> **用户管理系统（User Management）**

功能包括：

- 新增用户
- 查询用户列表
- 根据 ID 查询用户
- 删除用户

------

# 二、第一阶段：后端是什么？（世界观建立）

## 1. 什么是后端？

### 通俗解释

> 把一个系统想象成一家餐厅：

| 角色   | 对应   |
| ------ | ------ |
| 前端   | 服务员 |
| 后端   | 厨房   |
| 数据库 | 仓库   |

**后端的核心职责：**

- 接收请求
- 处理业务
- 操作数据库
- 返回结果

------

## 2. 一个接口请求完整流程（极其重要）

以“查询用户”为例：

1. 浏览器发请求
2. 后端 Controller 接收
3. Service 处理业务逻辑
4. Mapper 操作数据库
5. 返回数据给前端

### 通俗比喻

> 前端下单 → 后端接单 → 厨房做菜 → 仓库取料 → 上菜

------

# 三、第二阶段：Java 基础（只教后端必需）

> ⚠️ 不是 Java 教程，是“后端够用版”

------

## 1. Java 是什么？

- 一门编程语言
- 后端项目的“书写语言”

------

## 2. 类和对象（必讲）

```
class User {
    Long id;
    String name;
    Integer age;
}
```

### 类比

> class = 用户表设计图
>  object = 表里的一条数据

------

## 3. 方法是什么？

```
public void sayHello() {
    System.out.println("hello");
}
```

> 方法 = 一段可重复使用的“步骤说明书”

------

# 四、第三阶段：Spring Boot 是干嘛的？

## 1. 没有 Spring Boot 会怎样？

- 手动配置服务器
- 各种 XML
- 启动复杂

## 2. Spring Boot 的作用

> **让你 10 分钟跑起一个后端服务**

### 类比

> Spring Boot = 精装修毛坯房
>  你直接搬进去住

------

## 3. 第一个 Spring Boot 项目

### 3.1 使用 Spring Initializr

选择：

- Project：Maven
- Language：Java
- Spring Boot：稳定版
- Dependencies：
  - Spring Web
  - MyBatis Framework
  - MySQL Driver

------

## 3.2 项目结构详解（重点）

```
src/main/java
├─ controller
├─ service
├─ mapper
├─ entity
└─ Application.java
```

### 各层职责

| 层         | 作用       |
| ---------- | ---------- |
| Controller | 接收请求   |
| Service    | 业务逻辑   |
| Mapper     | 数据库操作 |
| Entity     | 数据模型   |

------

# 五、第四阶段：Controller（接口入口）

## 1. 什么是 Controller？

> Controller = 服务窗口
>  专门负责接单

------

## 2. 第一个接口

```
@RestController
@RequestMapping("/user")
public class UserController {

    @GetMapping("/hello")
    public String hello() {
        return "hello backend";
    }
}
```

### 注解解释（一定要讲）

| 注解            | 作用           |
| --------------- | -------------- |
| @RestController | 说明这是接口类 |
| @GetMapping     | 处理 GET 请求  |
| @RequestMapping | 请求路径前缀   |

------

## 3. 请求参数接收

```
@GetMapping("/get")
public User getUser(@RequestParam Long id) {
    ...
}
```

> @RequestParam = 从请求中取参数

------

# 六、第五阶段：Service（业务逻辑层）

## 1. 为什么不能把逻辑写 Controller？

### 问题

- Controller 变得臃肿
- 不好维护
- 不可复用

### 正确做法

> Controller 只接请求
>  Service 负责“怎么做”

------

## 2. Service 示例

```
@Service
public class UserService {

    public User getUserById(Long id) {
        // 业务逻辑
        return null;
    }
}
```

------

# 七、第六阶段：数据库与 MyBatis（核心）

## 1. 数据库是什么？

> 数据库 = 有规则的 Excel

------

## 2. 用户表设计

```
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50),
  age INT
);
```

------

## 3. Entity 编写

```
public class User {
    private Long id;
    private String name;
    private Integer age;
}
```

------

## 4. Mapper 接口

```
@Mapper
public interface UserMapper {

    User selectById(Long id);

    List<User> selectAll();

    int insert(User user);

    int deleteById(Long id);
}
```

------

## 5. Mapper XML

```
<mapper namespace="com.example.mapper.UserMapper">

    <select id="selectById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="User">
        SELECT * FROM user
    </select>

    <insert id="insert">
        INSERT INTO user(name, age)
        VALUES (#{name}, #{age})
    </insert>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>
```

### 通俗解释

> Mapper 接口 = 菜单
>  XML = 做菜步骤

------

# 八、第七阶段：完整项目串联（User 项目）

## 1. Controller

```
@RestController
@RequestMapping("/users")
public class UserController {

    @Autowired
    private UserService userService;

    @GetMapping
    public List<User> list() {
        return userService.getAllUsers();
    }

    @PostMapping
    public void add(@RequestBody User user) {
        userService.addUser(user);
    }
}
```

------

## 2. Service

```
@Service
public class UserService {

    @Autowired
    private UserMapper userMapper;

    public List<User> getAllUsers() {
        return userMapper.selectAll();
    }

    public void addUser(User user) {
        userMapper.insert(user);
    }
}
```

------

# 九、第八阶段：配置文件讲解（application.yml）

```
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
```

------

# 十、第九阶段：调试、错误与思维培养

## 1. 常见错误

- 端口冲突
- SQL 写错
- Mapper 扫描不到
- 404 接口

## 2. 教实习生如何排错

> 错误不是敌人
>  是路标

------

# 十一、第十阶段：总结与进阶方向

## 实习生最终能力

- 能写标准 Controller / Service / Mapper
- 能操作数据库
- 能看懂真实后端项目

## 后续进阶方向

- 参数校验
- 统一返回体
- 异常处理
- 登录鉴权
- Spring Security

------

# 十二、给导师的带教建议（非常重要）

> ❌ 不要一句话带过
>  ✅ 每一步让实习生“亲手跑通”

# 《后端实习生教学手册（上卷）》

**——从 0 到 1，用 Spring Boot + MyBatis 写出第一个后端项目**

------

## 写在最前面：给实习生的一段话（非常重要）

如果你正在读这份文档，说明你现在：

- 可能没写过后端代码
- 可能听不懂什么是 Controller / Service / Mapper
- 甚至不清楚“接口”是什么

**这都完全正常。**

后端不是天赋活，是一门**工程技能**，
 只要你愿意一步一步照着做，这份手册会把你带到：

> ✅ 能跑项目
>  ✅ 能看懂代码
>  ✅ 能自己改功能

------

# 第一章：后端到底是干什么的？（先把世界看清楚）

## 1.1 一个最直观的比喻

想象你在点外卖：

1. 你在手机上点菜（前端）
2. 商家系统接到订单（后端）
3. 厨房做菜（业务逻辑）
4. 仓库拿食材（数据库）
5. 做好后返回状态（响应）

在这个过程中：

- **你看不到厨房**
- 但厨房在默默完成所有核心工作

👉 后端，就是这个“厨房系统”。

------

## 1.2 后端工程师每天在干什么？

简单说，后端工程师主要做 4 件事：

1. **写接口**
    让前端能“请求数据”
2. **处理业务逻辑**
    比如：能不能下单、库存够不够
3. **操作数据库**
    增、删、改、查
4. **保证系统稳定**
    不崩、不乱、不出错

------

## 1.3 什么是“接口”？（新手必懂）

### 接口的本质

> 接口 = 一个可以被访问的“功能入口”

比如：

```
GET /users
```

意思是：

> “我想要所有用户的数据”

你不需要知道数据库怎么查的，
 你只关心：**给我结果**。

------

# 第二章：一次接口请求是怎么跑完整个后端的？

这是后端最重要的一章，**必须反复看**。

------

## 2.1 一次请求的完整生命周期

我们用“查询用户”为例：

1. 前端发请求：`GET /users/1`
2. Controller 接收请求
3. Service 处理业务逻辑
4. Mapper 查询数据库
5. 数据库返回结果
6. 后端返回 JSON 给前端

------

## 2.2 四层结构是怎么来的？

```
Controller → Service → Mapper → Database
```

### 为什么要分层？

想象一个混乱的厨房：

- 服务员、厨师、采购都挤在一起
- 谁都能乱动锅

👉 **分层的目的只有一个：不乱**

------

# 第三章：Java 是什么？你需要会到什么程度？

## 3.1 不要被 Java 吓到

你现在不需要：

- 精通 JVM
- 会多线程
- 背 API

你只需要会：

- 类
- 方法
- 基本语法
- 看懂代码

------

## 3.2 类（Class）到底是什么？

```
public class User {
    private Long id;
    private String name;
    private Integer age;
}
```

### 用人话解释

> class = 一张表结构说明
>  object = 表里的一行数据

------

## 3.3 方法（Method）是什么？

```
public void sayHello() {
    System.out.println("hello");
}
```

> 方法 = 一段“可重复使用的操作步骤”

------

# 第四章：Spring Boot 是干嘛的？（核心）

## 4.1 如果没有 Spring Boot

你要：

- 手动写服务器配置
- 手动处理请求
- 管理大量 XML

👉 新人基本直接劝退

------

## 4.2 Spring Boot 帮你做了什么？

一句话总结：

> **Spring Boot 把“麻烦事”全做完了**

你只需要：

- 写业务代码
- 写接口逻辑

------

## 4.3 创建第一个 Spring Boot 项目（一步不跳）

### 4.3.1 使用 Spring Initializr

选择：

- Project：Maven
- Language：Java
- Packaging：Jar
- Dependencies：
  - Spring Web
  - MyBatis Framework
  - MySQL Driver

------

## 4.3.2 项目启动类解读

```
@SpringBootApplication
public class DemoApplication {
    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }
}
```

### 解释

> 这是整个后端项目的“启动按钮”

------

# 第五章：项目结构详解（新手最容易迷路）

```
src/main/java
├─ controller
├─ service
├─ mapper
├─ entity
└─ DemoApplication.java
```

------

## 5.1 每一层只干一件事

| 层         | 职责     |
| ---------- | -------- |
| Controller | 接请求   |
| Service    | 业务处理 |
| Mapper     | 查数据库 |
| Entity     | 数据模型 |

> ❌ 不要混着写
>  ✅ 一眼就知道代码干嘛的

------

# 第六章：Controller —— 接口的入口

## 6.1 Controller 是什么？

> Controller = 前台窗口
>  专门接用户请求

------

## 6.2 第一个接口（你人生第一个后端接口）

```
@RestController
@RequestMapping("/hello")
public class HelloController {

    @GetMapping
    public String hello() {
        return "hello backend";
    }
}
```

### 逐行解释

- `@RestController`
   👉 告诉 Spring：这是接口类
- `@GetMapping`
   👉 接收 GET 请求
- return
   👉 返回给前端的内容

------

## 6.3 如何访问？

浏览器访问：

```
http://localhost:8080/hello
```

看到返回值，说明你已经**真正写出了后端接口**。

------

# 第七章：参数是怎么传进来的？

## 7.1 查询参数

```
@GetMapping("/get")
public String get(@RequestParam String name) {
    return name;
}
```

访问：

```
/get?name=Tom
```

------

## 7.2 请求体（JSON）

```
@PostMapping("/add")
public String add(@RequestBody User user) {
    return user.getName();
}
```

> `@RequestBody` = 把 JSON 转成 Java 对象

------

# 第八章：Service —— 真正干活的地方

## 8.1 为什么要有 Service？

如果所有逻辑都写 Controller：

- 接口类会越来越大
- 逻辑难复用
- 后期维护噩梦

👉 **Service 是“干活层”**

------

## 8.2 Service 示例

```
@Service
public class UserService {

    public String process() {
        return "处理中";
    }
}
```

> Controller 负责“接”
>  Service 负责“做”

------

# 第九章：数据库基础（必须懂）

## 9.1 数据库是什么？

> 数据库 = 有规则的 Excel

- 表 = Sheet
- 行 = 一条数据
- 列 = 一个字段

------

## 9.2 用户表设计

```
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50),
  age INT
);
```

------

## 9.3 Entity 的作用

```
public class User {
    private Long id;
    private String name;
    private Integer age;
}
```

> Entity = Java 世界里的“一行数据”

------

> —— **上卷到此结束** ——

# 《后端实习生教学手册（下卷）》

**——MyBatis、数据库、完整项目与真实后端思维**

------

# 第十章：MyBatis 是什么？（新手最容易懵的一章）

## 10.1 先说结论：MyBatis 干嘛的？

一句话：

> **MyBatis = 帮你把 Java 和 SQL 连起来**

------

## 10.2 为什么不能直接在 Java 里写 SQL？

你当然可以这样写（但不推荐）：

```
String sql = "SELECT * FROM user WHERE id = " + id;
```

问题：

- SQL 和 Java 混在一起
- 不好维护
- 容易 SQL 注入
- 项目一大直接失控

------

## 10.3 MyBatis 的设计思想（非常重要）

MyBatis 把事情分成两块：

| 部分        | 做什么                |
| ----------- | --------------------- |
| Mapper 接口 | 定义“我要做什么”      |
| Mapper XML  | 定义“具体 SQL 怎么写” |

### 通俗比喻

> Mapper 接口 = 菜单
>  Mapper XML = 厨师做菜步骤

------

# 第十一章：MyBatis 核心结构拆解

## 11.1 Mapper 接口长什么样？

```
@Mapper
public interface UserMapper {

    User selectById(Long id);

    List<User> selectAll();

    int insert(User user);

    int deleteById(Long id);
}
```

### 你要看懂什么？

- **方法名**：你要干什么
- **参数**：SQL 需要什么
- **返回值**：SQL 查出来什么

------

## 11.2 Mapper XML 是什么？

```
<mapper namespace="com.example.mapper.UserMapper">

    <select id="selectById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

</mapper>
```

### 关键点逐条解释

| 内容      | 含义             |
| --------- | ---------------- |
| namespace | 对应 Mapper 接口 |
| id        | 对应接口方法名   |
| #{id}     | 方法参数         |

> ❗ XML 里的 `id` **必须和接口方法名一致**

------

## 11.3 参数是怎么传进去的？

```
User selectById(Long id);
```

SQL：

```
SELECT * FROM user WHERE id = #{id}
```

👉 MyBatis 自动把 `id` 塞进 SQL

------

# 第十二章：application.yml 配置详解（新手必卡点）

```
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.entity
```

------

## 12.1 每一项都在干嘛？

### datasource

- 告诉 Spring：
  - 数据库地址
  - 用户名
  - 密码

### mybatis.mapper-locations

- 告诉 MyBatis：
  - XML 在哪

### type-aliases-package

- 让 XML 里可以直接写 `User`
- 不用写全类名

------

# 第十三章：完整用户管理项目（从 0 到可用）

下面是**一个完整、最小但规范的后端项目结构**。

------

## 13.1 项目结构总览

```
src/main/java/com/example
├─ controller
│  └─ UserController.java
├─ service
│  └─ UserService.java
├─ mapper
│  └─ UserMapper.java
├─ entity
│  └─ User.java
└─ DemoApplication.java
```

------

# 第十四章：完整代码逐层讲解（核心）

------

## 14.1 Entity：User.java

```
public class User {

    private Long id;
    private String name;
    private Integer age;

    // getter / setter
}
```

### 实习生必须理解的点

- Entity ≠ 表
- Entity = Java 世界里的数据对象
- 一般字段名和表字段一致

------

## 14.2 Mapper 接口

```
@Mapper
public interface UserMapper {

    List<User> selectAll();

    User selectById(Long id);

    int insert(User user);

    int deleteById(Long id);
}
```

> Mapper 不写实现
>  MyBatis 帮你“生成实现类”

------

## 14.3 Mapper XML

```
<mapper namespace="com.example.mapper.UserMapper">

    <select id="selectAll" resultType="User">
        SELECT * FROM user
    </select>

    <select id="selectById" resultType="User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO user(name, age)
        VALUES (#{name}, #{age})
    </insert>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>
```

------

## 14.4 Service 层

```
@Service
public class UserService {

    @Autowired
    private UserMapper userMapper;

    public List<User> listUsers() {
        return userMapper.selectAll();
    }

    public User getUser(Long id) {
        return userMapper.selectById(id);
    }

    public void addUser(User user) {
        userMapper.insert(user);
    }

    public void deleteUser(Long id) {
        userMapper.deleteById(id);
    }
}
```

### Service 的核心职责

- 组合 Mapper
- 写业务判断
- 不写 SQL

------

## 14.5 Controller 层

```
@RestController
@RequestMapping("/users")
public class UserController {

    @Autowired
    private UserService userService;

    @GetMapping
    public List<User> list() {
        return userService.listUsers();
    }

    @GetMapping("/{id}")
    public User get(@PathVariable Long id) {
        return userService.getUser(id);
    }

    @PostMapping
    public void add(@RequestBody User user) {
        userService.addUser(user);
    }

    @DeleteMapping("/{id}")
    public void delete(@PathVariable Long id) {
        userService.deleteUser(id);
    }
}
```

------

# 第十五章：一次真实请求的完整链路回放

以 `GET /users/1` 为例：

1. 浏览器访问接口
2. Controller 接收请求
3. Service 调用 Mapper
4. Mapper 执行 SQL
5. 数据库返回结果
6. 后端返回 JSON

> 🎯 实习生一定要在脑子里能“放电影”

------

# 第十六章：后端新手最常见错误（非常重要）

## 16.1 接口 404

- 路径写错
- Controller 没加注解
- 端口不对

------

## 16.2 Mapper 找不到

- XML 路径没配置
- namespace 写错
- 方法名不一致

------

## 16.3 数据查不到

- SQL 自己在数据库跑一遍
- 不要“凭感觉改”

------

# 第十七章：给实习生的后端思维训练

## 17.1 写代码前先问自己 3 个问题

1. 这个接口干嘛？
2. 数据从哪来？
3. 最终返回什么？

------

## 17.2 后端不是“写接口机器”

你要慢慢建立：

- 结构感
- 责任边界
- 可维护意识

------

# 第十八章：下一步进阶路线（实习期后半段）

等实习生能熟练写 CRUD 后，可以逐步引入：

- 统一返回对象
- 参数校验（@Valid）
- 异常处理
- 日志
- 登录鉴权
- 分页

------

# 第十九章：给导师的实战带教建议（非常重要）

> ❌ 不要替实习生改代码
>  ✅ 让他自己跑、自己错、自己改

你只负责：

- 指方向
- 给思路
- 保证不走偏