# 一、整体培养目标与路线设计

## 1. 培养目标（你要把实习生带到什么程度）

经过本教学计划，实习生应当具备以下能力：

1. 明白前端是做什么的、在整个系统中的位置
2. 理解浏览器如何加载和运行前端代码
3. 能够独立：
   - 搭建 Vue + TypeScript 项目
   - 理解项目目录结构
   - 编写组件
   - 使用响应式数据
   - 实现基础交互逻辑
   - 调试和排错
4. 具备**继续自学前端的能力**

------

## 2. 教学路线总览（先给实习生一张“地图”）

> 类比：
>  学前端就像学开车
>
> - 先认识方向盘、油门、刹车
> - 再学起步
> - 然后上路
> - 最后能自己导航去目的地

### 阶段划分

| 阶段     | 内容                 | 类比       |
| -------- | -------------------- | ---------- |
| 第一阶段 | 前端 & Web 基础认知  | 认识汽车   |
| 第二阶段 | HTML / CSS / JS 基础 | 学会踩油门 |
| 第三阶段 | Vue 框架入门         | 自动挡     |
| 第四阶段 | TypeScript           | 行车规则   |
| 第五阶段 | 项目工程化           | 修一条路   |
| 第六阶段 | 完整项目实战         | 真正上路   |
| 第七阶段 | 总结 & 提升          | 独立驾驶   |

------

# 二、第一阶段：前端是什么？（认知建立）

## 1. 什么是前端？（非常重要）

### 通俗解释

> 把一个网站想象成一家餐厅：
>
> - 前端：服务员 + 菜单 + 装修
> - 后端：厨师
> - 数据库：仓库

**前端负责用户“看到和点到”的一切**

### 前端主要做什么？

- 页面展示（文字、图片、按钮）
- 用户交互（点击、输入、拖拽）
- 向后端要数据
- 把数据“翻译”成好看的页面

------

## 2. 浏览器是如何工作的（必须理解）

### 一个页面打开时发生了什么？

1. 用户输入网址
2. 浏览器请求服务器
3. 服务器返回 HTML / CSS / JS
4. 浏览器解析代码
5. 页面显示出来

### 通俗比喻

> 浏览器 = 机器人
>  HTML = 骨架
>  CSS = 皮肤
>  JS = 大脑

------

# 三、第二阶段：前端三大基础（HTML / CSS / JavaScript）

> ⚠️ 虽然最终用 Vue，但**基础一定要讲**

------

## 1. HTML：页面的骨架

### 1.1 HTML 是什么？

HTML 是一种**描述页面结构的语言**

### 示例

```
<!DOCTYPE html>
<html>
  <head>
    <title>我的第一个页面</title>
  </head>
  <body>
    <h1>你好，前端</h1>
    <p>这是一个段落</p>
    <button>点我</button>
  </body>
</html>
```

### 类比

> HTML 就像搭房子的砖和钢筋
>  不好看，但不能没有

------

## 2. CSS：让页面变好看

### 2.1 CSS 是什么？

CSS 用来控制样式：颜色、大小、布局

### 示例

```
h1 {
  color: red;
  font-size: 24px;
}
```

### 类比

> CSS 是装修
>  没装修也能住，但不好看

------

## 3. JavaScript：让页面“动起来”

### 3.1 JS 是什么？

JavaScript 是一门编程语言，用来：

- 处理点击
- 操作数据
- 控制页面行为

### 示例

```
button.onclick = function () {
  alert('你点了按钮')
}
```

### 类比

> JS 是大脑
>  决定你点按钮之后会发生什么

------

# 四、第三阶段：为什么要用 Vue？

## 1. 原生开发的问题

### 问题描述

- 页面复杂后，代码混乱
- DOM 操作麻烦
- 状态难以维护

### 类比

> 原生 JS 就像手写汇编
>  Vue 就像高级语言

------

## 2. Vue 是什么？

Vue 是一个**前端框架**，帮我们：

- 管理数据
- 自动更新页面
- 拆分组件

### Vue 核心思想

> **数据驱动视图**

------

# 五、第四阶段：Vue 3 项目从 0 搭建

## 1. 开发环境准备

### 1.1 安装 Node.js

- Node.js 是前端的“运行环境”
- 下载 LTS 版本

验证：

```
node -v
npm -v
```

------

## 2. 使用 Vite 创建 Vue + TS 项目

```
npm create vite@latest todo-app
```

选择：

- Framework: Vue
- Variant: TypeScript

进入项目：

```
cd todo-app
npm install
npm run dev
```

------

## 3. 项目目录结构讲解（非常重要）

```
src/
├─ main.ts        # 项目入口
├─ App.vue        # 根组件
├─ components/    # 公共组件
├─ assets/        # 静态资源
```

### 类比

> src 就像工地
>  main.ts 是大门
>  App.vue 是主楼

------

# 六、第五阶段：Vue 组件与语法核心

## 1. 什么是组件？

### 通俗解释

> 组件 = 可复用的页面零件
>  就像乐高积木

------

## 2. Vue 单文件组件结构

```
<template>
  <div>Hello</div>
</template>

<script setup lang="ts">
</script>

<style scoped>
</style>
```

### 三段式含义

| 部分     | 作用    |
| -------- | ------- |
| template | HTML    |
| script   | JS / TS |
| style    | CSS     |

------

## 3. 响应式数据（Vue 的灵魂）

```
import { ref } from 'vue'

const count = ref(0)

count.value++
```

### 类比

> ref 就像一个“会自动刷新的变量”

------

## 4. 事件绑定

```
<button @click="add">+</button>
```

------

# 七、第六阶段：TypeScript 基础（前端工程师的护栏）

## 1. 为什么要用 TypeScript？

### 问题

```
function add(a, b) {
  return a + b
}
```

### TS 解决方案

```
function add(a: number, b: number): number {
  return a + b
}
```

### 类比

> TS 就像交通规则
>  限制你乱开车

------

## 2. 常见类型

```
string
number
boolean
array
interface
```

------

# 八、第七阶段：实战项目 —— Todo 管理系统

## 1. 项目功能拆解

- 输入待办事项
- 添加事项
- 删除事项
- 列表渲染

------

## 2. 数据结构设计

```
interface Todo {
  id: number
  title: string
  done: boolean
}
```

------

## 3. 列表渲染

```
<li v-for="todo in todos" :key="todo.id">
  {{ todo.title }}
</li>
```

------

## 4. 添加逻辑

```
const todos = ref<Todo[]>([])

function addTodo() {
  todos.value.push({
    id: Date.now(),
    title: input.value,
    done: false
  })
}
```

------

# 九、第八阶段：调试、错误与思维培养

## 1. 常见错误

- 控制台报错不看
- 数据类型不匹配
- 响应式使用错误

## 2. 教实习生如何“查错”

> 不会不可怕
>  不敢查才可怕

------

# 十、第九阶段：总结与进阶方向

## 实习生最终收获

- 不再害怕前端
- 能读懂 Vue 项目
- 能自己搭项目
- 具备继续深入的基础

## 后续学习方向

- Vue Router
- Pinia
- 接口请求
- 工程规范
- 性能优化

------

## 结语（带教建议）

> 教实习生不是“甩文档”
>  而是：
>
> - 给地图
> - 给路标
> - 给安全感

# 一、完整教学手册（从 0 到 1，可独立照做）

> 🎯 教学目标
>  一个**完全零基础**的实习生，只要：
>
> - 会基本电脑操作
> - 会复制粘贴命令
>
> **就可以：**
>
> - 独立搭建 Vue + TypeScript 项目
> - 理解每一行代码“在干什么”
> - 完成一个可运行、可维护的 Todo 项目

------

## 第 1 章：前端到底在做什么（先建立“世界观”）

### 1.1 前端工程师的职责

一句话版本：

> **前端工程师 = 把数据变成用户能看懂、能操作的界面**

更直白一点：

- 后端给你一堆数据（JSON）
- 前端负责：
  - 摆页面
  - 接按钮
  - 处理点击
  - 告诉后端“用户想干嘛”

------

### 1.2 浏览器是怎么运行前端代码的？

浏览器只认识三种东西：

| 文件 | 作用     |
| ---- | -------- |
| HTML | 页面结构 |
| CSS  | 样式     |
| JS   | 逻辑     |

👉 Vue、TypeScript **最终都会被转换成 JS**

------

## 第 2 章：Vue 是干嘛的？（为什么不用原生）

### 2.1 原生 JS 的问题

- 手动操作 DOM 很痛苦
- 数据变化要自己刷新页面
- 项目一大就乱

### 2.2 Vue 的核心思想（非常重要）

> **数据变了，页面自动变**

你不再关心：

- 怎么改 DOM
- 什么时候刷新页面

你只关心：

- 数据是什么
- 用户点了什么

------

## 第 3 章：准备开发环境（一步一步）

### 3.1 安装 Node.js

- 下载 **LTS 版本**
- 安装完成后打开终端：

```
node -v
npm -v
```

能看到版本号即可。

------

### 3.2 创建 Vue + TypeScript 项目

```
npm create vite@latest todo-project
```

选择：

- Framework → Vue
- Variant → TypeScript

然后：

```
cd todo-project
npm install
npm run dev
```

浏览器看到页面，说明成功。

------

## 第 4 章：项目结构详解（新人最容易迷）

```
todo-project/
├─ src/
│  ├─ main.ts
│  ├─ App.vue
│  ├─ components/
│  └─ assets/
└─ index.html
```

### 4.1 main.ts 是什么？

```
import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
```

**解释：**

> 把 App.vue 当作整个页面的入口
>  挂载到 index.html 的 #app 上

------

### 4.2 App.vue 是什么？

> App.vue = 整个应用的“根组件”

------

## 第 5 章：Vue 单文件组件（SFC）

### 5.1 组件的三段结构

```
<template></template>
<script setup lang="ts"></script>
<style scoped></style>
```

| 区域     | 作用    |
| -------- | ------- |
| template | 写 HTML |
| script   | 写逻辑  |
| style    | 写样式  |

------

## 第 6 章：响应式数据（Vue 的灵魂）

### 6.1 ref 是什么？

```
import { ref } from 'vue'

const count = ref(0)
```

解释：

- `count` 不是普通变量
- `.value` 才是真实值
- Vue 会“监听”它的变化

------

### 6.2 为什么要这样设计？

> Vue 要知道 **什么时候更新页面**

------

## 第 7 章：TypeScript 基础（只教必须的）

### 7.1 interface 是什么？

```
interface Todo {
  id: number
  title: string
  done: boolean
}
```

**解释：**

> interface 就是“数据说明书”

------

## 第 8 章：Todo 项目实战（完整）

------

# 二、完整 Todo 项目源码（可直接运行）

------

## 1️⃣ App.vue（完整）

```
<template>
  <div class="app">
    <h1>Todo List</h1>

    <!-- 输入区域 -->
    <div class="input-area">
      <input
        v-model="newTodo"
        placeholder="请输入待办事项"
        @keyup.enter="addTodo"
      />
      <button @click="addTodo">添加</button>
    </div>

    <!-- 列表区域 -->
    <ul class="todo-list">
      <li v-for="todo in todos" :key="todo.id">
        <span :class="{ done: todo.done }">
          {{ todo.title }}
        </span>

        <div class="actions">
          <button @click="toggleTodo(todo.id)">
            {{ todo.done ? '未完成' : '完成' }}
          </button>
          <button @click="removeTodo(todo.id)">删除</button>
        </div>
      </li>
    </ul>

    <p v-if="todos.length === 0">暂无待办事项</p>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

/**
 * Todo 数据结构说明
 */
interface Todo {
  id: number
  title: string
  done: boolean
}

/**
 * 新输入的 todo 文本
 */
const newTodo = ref('')

/**
 * todo 列表
 */
const todos = ref<Todo[]>([])

/**
 * 添加 todo
 */
function addTodo() {
  if (!newTodo.value.trim()) return

  todos.value.push({
    id: Date.now(),
    title: newTodo.value,
    done: false
  })

  newTodo.value = ''
}

/**
 * 删除 todo
 */
function removeTodo(id: number) {
  todos.value = todos.value.filter(todo => todo.id !== id)
}

/**
 * 切换完成状态
 */
function toggleTodo(id: number) {
  const todo = todos.value.find(todo => todo.id === id)
  if (todo) {
    todo.done = !todo.done
  }
}
</script>

<style scoped>
.app {
  max-width: 500px;
  margin: 50px auto;
  font-family: Arial, sans-serif;
}

.input-area {
  display: flex;
  gap: 10px;
}

input {
  flex: 1;
  padding: 8px;
}

button {
  padding: 6px 10px;
  cursor: pointer;
}

.todo-list {
  margin-top: 20px;
  list-style: none;
  padding: 0;
}

.todo-list li {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
}

.done {
  text-decoration: line-through;
  color: gray;
}

.actions button {
  margin-left: 5px;
}
</style>
```

------

# 三、逐段讲解（给实习生看的“人话版”）

------

## 1. v-model 是什么？

```
<input v-model="newTodo" />
```

等价于：

- 输入框变化 → `newTodo` 变化
- `newTodo` 变化 → 输入框变化

👉 **双向绑定**

------

## 2. v-for 是什么？

```
<li v-for="todo in todos" :key="todo.id">
```

意思：

> 对 todos 数组里的每一项
>  都生成一个 li

------

## 3. @click 是什么？

```
<button @click="addTodo">添加</button>
```

意思：

> 用户点击 → 执行函数

------

## 4. 为什么用 ref<Todo[]>？

```
const todos = ref<Todo[]>([])
```

解释：

- `Todo[]`：数组里只能放 Todo
- `ref`：让 Vue 监听变化

------

## 5. 为什么不能直接改数组？

```
todos.value = todos.value.filter(...)
```

👉 因为 Vue 监听的是 `.value`

------

# 四、你作为导师可以怎么用这套东西

### 推荐教学方式

| 天数  | 内容               |
| ----- | ------------------ |
| Day 1 | 环境 + Vue 基础    |
| Day 2 | ref / 事件 / 列表  |
| Day 3 | TypeScript         |
| Day 4 | Todo 项目          |
| Day 5 | 让实习生自己改功能 |